// Bottle Return
// 사칙연산

#include <iostream>

int main() {

  std::ios_base::sync_with_stdio(0);
  std::cin.tie(0);

  int a, b, c, d, e, f;

  std::cin >> a >> b >> c >> d >> e >> f;
  std::cout << (a + b + c + d + e + f) * 5 << '\n';

  return 0;
}